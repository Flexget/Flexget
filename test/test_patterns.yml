# unittest

feeds:
  test:
    # make test data
    input_mock:
      # matched by patterns
      - {title: 'pattern', url: 'http://localhost/pattern', 'imdb_score': 5}
      # filtered by patterns, accepted by accept
      - {title: 'accept', url: 'http://localhost/accept'}
      # unmatched
      - {title: 'unmatched', url: 'http://localhost/unmatched'}
      # not
      - {title: 'foobar', url: 'http://localhost/foobar'}
      - {title: 'xxx.yyyy', url: 'http://localhost/xxx.yyyy', 'imdb_score': 5}
    patterns:
      - pattern
      - foo: {not: bar}
      - xxx: {not: asdf}
    accept:
      - accept