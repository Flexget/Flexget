# unittest

feeds:
  test:
    # make test data
    input_mock:
      # matched by patterns
      - {title: 'pattern', url: 'http://localhost/pattern'}
      # filtered by patterns, accepted by accept
      - {title: 'accept', url: 'http://localhost/accept'}
      # unmatched
      - {title: 'unmatched', url: 'http://localhost/unmatched'}
    patterns:
      - pattern
    accept:
      - accept