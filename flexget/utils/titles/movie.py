import logging
import re

from flexget.utils.titles.parser import TitleParser, ParseWarning

log = logging.getLogger('movieparser')

class MovieParser(TitleParser):
    # TODO: implement, see flexget.utils.imdb ImdbSearch.parse_name()
    pass

