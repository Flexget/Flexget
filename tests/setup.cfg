[nosetests]
verbosity = 2
detailed-errors = 1
with-id = 1
# coverage- requires coverage: pip install coverage
with-coverage = 1
cover-package = flexget
cover-branches = 1
cover-html = 1
# logging during tests
quiet = 1
logging-config = logging.cfg
