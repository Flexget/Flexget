This directory contains FlexGet unit tests and related resources. These can only be ran in development
sandbox.

To run all tests:
-----------------

bin/paver test

Use --online to enable online tests.


To run a single test suite:
---------------------------

bin/nosetests tests/test_rss.py:TestInputRSS -v


To run a single test in a suite:
--------------------------------

bin/nosetests tests/test_rss.py:TestInputRSS.test_rss -v


Test parameters can be added with --atrr=<params>

Ie. to disable online tests: --attr=\!online