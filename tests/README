This directory contains test configuration files for modules. Usefull when developing with flexget.

Example:

python flexget.py -d -c test/series.yml

Ideally in the future there should be one configuration file per each module that is also used for unit testing.
