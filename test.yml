#
# download locations
#

locations:

  test: &test
    location: ~/torrents_test/

feeds:

  dummysource:
    test: http://foo.bar/com
    html:
      url: http://www.sektori.com
    seen: foo
    patterns:
      - dummy
      - sektori
    imdb:
      min_score: 3.0
      min_votes: 5000
      reject_genres:
        - horror
        - documentary
    download: *test
