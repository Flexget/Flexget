#
# download locations
#

locations:

  test: &test
    location: ~/torrents_test/

global:
  seen: None

feeds:

  dummysource:
    test: http://foo.bar/com
    patterns:
      - dummy:
          location: ~/custom/
          not: [9, 8]
    unconditionally:
      - 9
    dump: Yes please
